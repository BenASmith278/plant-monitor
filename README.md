# Smart Garden

## How To:

On LAMPI: Run main.py. Make sure requirements.txt is installed, and the EC2 instance IP address and Serial port are properly configured in the file. To find which Serial port is in use, run ls /dev/tty*.

On EC2: Run /smartgarden/manage.py runserver 0.0.0.0:8000.
